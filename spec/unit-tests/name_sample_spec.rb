require 'name_sample'

describe Name_Sample do 

  let(:randomiser) { described_class.new}

describe "#samples" do
    it "Creates a new instance of random" do
      expect(game.player).to eq player
    end
  end
end